# Python virtual environment
.venv/
__pycache__/
*.pyc

# Raw PDF files — markdown extracts are committed instead
*.pdf

# Obsidian
.obsidian/
*.canvas

# .gitkeep files — not needed in version control
*.gitkeep

# Orphan images moved out of wiki/ — ignored, kept only for local reference
Trading_Wiki/images_not_mentioned/

# Raw and graphify output directories
raw/
graphify-out/
